" .vimrc

set nocompatible
filetype off

" Vundle
so ~/.vim/config/plugins.vim

" Plugins Config
so ~/.vim/config/plugins-config.vim

" Keymaps
so ~/.vim/config/keymaps.vim

" Settings
so ~/.vim/config/settings.vim

" Color and Font
so ~/.vim/config/color.vim
